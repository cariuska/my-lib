from setuptools import setup

setup(
    name="my-lib",
    version="0.0.1",
    packages=[],
    python_requires='>=3.6.0',
    install_requires=[
        "requests"
    ],
    extras_require={
    }
)
